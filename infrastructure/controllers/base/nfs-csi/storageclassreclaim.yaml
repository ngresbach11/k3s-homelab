apiVersion: storage.k8s.io/v1
kind: storageClass
metadata:
  name: nfs-csi
provisioner: nfs.csi.k8s.io
parameters:
  server: 10.0.0.3
  share: /mnt/user/k8s-storage
reclaimPolicy: Recalim
volumeBindingMode: Immediate
mountOptions:
  - hard
  - nfsvers=4