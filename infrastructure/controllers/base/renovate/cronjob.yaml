apiVersion: batch/v1
kind: CronJob
metadata:
  name: renovate
  namespace: renovate
spec:
  schedule: "@hourly"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: renovate
              # renovate: datasource=docker depName=renovate/renovate
              image: renovate/renovate:43.48.2
              args:
                - ngresbach11/k3s-homelab
              envFrom:
                - secretRef:
                    name: renovate-token
                - configMapRef:
                    name: renovate-configmap
          restartPolicy: Never